import 'package:flutter/cupertino.dart';

class UserWordBookManagementSubPage extends StatelessWidget {
  const UserWordBookManagementSubPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("用户单词书管理"));
  }
}
